Video : https://www.youtube.com/watch?v=cLbsRJsKjWE

COMANDO: 
GET_MOST_POPULOUS_GENDER_BY_COUNTRY <countries-list>

Function: 
O comando GET_MOST_POPULOUS_GENDER_BY_COUNTRY tem uma entrada de uma lista de paises, Sendo assim usando o ano atual e os paises introduzidos o nosso comando vê qual a população mais predominante se é masculina ou feminina e apresenta esses resultados.


GPT LOG:

Prompt:
How Map Entry works

Result:
A Map.Entry in Java is an interface that represents an entry in a map, which is a collection of key-value pairs. A Map.Entry has methods to get the key and value, and to check if it's equal to another entry. It's used to iterate over the entries in a map, or to create custom entries for use in a map.

Prompt: 
how AbstractMap.SimpleEntry works

Result:
AbstractMap.SimpleEntry is a class in Java that implements the Map.Entry interface. It represents a key-value pair and is often used in maps. You can create a new instance by providing a key and a value, like so: Map.Entry<String, Integer> entry = new AbstractMap.SimpleEntry<>("apple", 1);. The key is "apple" and the value is 1 in this example. You can retrieve the key and value using the getKey() and getValue() methods, respectively.